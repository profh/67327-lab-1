<!DOCTYPE html>
<html>
<head>
  <title>The Chess Store - A Damn Vulnerable Rails App</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
	<!-- Guys, I set up a default 'admin' account automatically for us to use in testing and the like.  -->
</head>
<body>
	
	<%= render :partial => 'layouts/info_bar' %>
	
	<div id="wrapper">
		
		<div id="content">
			<%= render :partial => 'layouts/header' %>
			<%= render :partial => 'layouts/navigation' %>
			<%= breadcrumbs(nil) %>
			<%= render :partial => 'layouts/flash', :locals => { :flash => flash } %>
			
			<%= yield %>
			
			<div class="clear"></div>
		</div>
		
		<%= render :partial => 'layouts/footer' %>
	</div>

</body>
</html>
